spring:
  application:
    name: bydzov-ctverec-api
  datasource:
    hikari:
      connection-timeout: 30000
      max-lifetime: 600000
      maximum-pool-size: 5
      minimum-idle: 0
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
